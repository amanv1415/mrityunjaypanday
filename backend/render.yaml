services:
  - type: web
    name: backend
    env: node
    buildCommand: "npm install"
    startCommand: "node src/server.js"
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
