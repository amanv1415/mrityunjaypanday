# Public API base URL used by the frontend for backend requests.
# Local development default is http://localhost:5000 if unset.
NEXT_PUBLIC_API_BASE=

# Optional personalization on the hero section.
NEXT_PUBLIC_TARGET_COMPANY=
